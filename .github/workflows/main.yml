name: Terraform Production Pipeline

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:

  # ============================================================
  # 1. SECURITY SCAN
  # ============================================================
  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ---------------- Gitleaks ----------------
      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # ---------------- Terraform ----------------
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.14.7

      # ---------------- TFLint ----------------
      - name: Setup TFLint
        uses: terraform-linters/setup-tflint@v4
        with:
          tflint_version: v0.64.0

      - name: Run TFLint
        working-directory: ./parent
        run: |
          tflint --init
          tflint

      # ---------------- tfsec ----------------
      - name: Run tfsec
        uses: aquasecurity/tfsec-action@v1.0.3
        with:
          working_directory: ./parent


  # ============================================================
  # 2. TERRAFORM PLAN
  # ============================================================
  terraform-plan:
    name: Terraform Plan
    runs-on: ubuntu-latest
<<<<<<< HEAD
=======

>>>>>>> 0f12c11 (Add Terraform plan approval and apply pipeline)
    needs: security-scan

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

<<<<<<< HEAD
=======
      # ---------------- Terraform ----------------
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.14.7

>>>>>>> 0f12c11 (Add Terraform plan approval and apply pipeline)
      # ---------------- Azure Login ----------------
      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: |
            {
              "clientId": "${{ secrets.AZURE_CLIENT_ID }}",
              "clientSecret": "${{ secrets.AZURE_CLIENT_SECRET }}",
              "subscriptionId": "${{ secrets.AZURE_SUBSCRIPTION_ID }}",
              "tenantId": "${{ secrets.AZURE_TENANT_ID }}"
            }

<<<<<<< HEAD
      # ---------------- Terraform ----------------
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.14.7

      # ---------------- Terraform Init ----------------
      - name: Terraform Init
        working-directory: ./parent
=======
      # ---------------- Terraform Init ----------------
      - name: Terraform Init
        working-directory: ./parent
        env:
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
>>>>>>> 0f12c11 (Add Terraform plan approval and apply pipeline)
        run: terraform init

      # ---------------- Terraform Validate ----------------
      - name: Terraform Validate
        working-directory: ./parent
<<<<<<< HEAD
=======
        env:
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
>>>>>>> 0f12c11 (Add Terraform plan approval and apply pipeline)
        run: terraform validate

      # ---------------- Terraform Plan ----------------
      - name: Terraform Plan
        working-directory: ./parent
<<<<<<< HEAD
=======
        env:
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          TF_VAR_vm_admin_password: ${{ secrets.AZURE_VM_ADMIN_PASSWORD }}
>>>>>>> 0f12c11 (Add Terraform plan approval and apply pipeline)
        run: terraform plan -out=tfplan

      # ---------------- Upload Plan ----------------
      - name: Upload Terraform Plan
        uses: actions/upload-artifact@v4
        with:
          name: terraform-plan
          path: ./parent/tfplan


  # ============================================================
<<<<<<< HEAD
  # 3. MANUAL APPROVAL + APPLY
=======
  # 3. MANUAL APPROVAL + TERRAFORM APPLY
>>>>>>> 0f12c11 (Add Terraform plan approval and apply pipeline)
  # ============================================================
  terraform-apply:
    name: Terraform Apply
    runs-on: ubuntu-latest
<<<<<<< HEAD
    needs: terraform-plan

=======

    needs: terraform-plan

    # Manual approval happens HERE, after Plan succeeds
>>>>>>> 0f12c11 (Add Terraform plan approval and apply pipeline)
    environment:
      name: production

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

<<<<<<< HEAD
=======
      # ---------------- Terraform ----------------
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.14.7

>>>>>>> 0f12c11 (Add Terraform plan approval and apply pipeline)
      # ---------------- Azure Login ----------------
      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: |
            {
              "clientId": "${{ secrets.AZURE_CLIENT_ID }}",
              "clientSecret": "${{ secrets.AZURE_CLIENT_SECRET }}",
              "subscriptionId": "${{ secrets.AZURE_SUBSCRIPTION_ID }}",
              "tenantId": "${{ secrets.AZURE_TENANT_ID }}"
            }

<<<<<<< HEAD
      # ---------------- Terraform ----------------
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.14.7

      # ---------------- Terraform Init ----------------
      - name: Terraform Init
        working-directory: ./parent
=======
      # ---------------- Terraform Init ----------------
      - name: Terraform Init
        working-directory: ./parent
        env:
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
>>>>>>> 0f12c11 (Add Terraform plan approval and apply pipeline)
        run: terraform init

      # ---------------- Download Plan ----------------
      - name: Download Terraform Plan
        uses: actions/download-artifact@v4
        with:
          name: terraform-plan
          path: ./parent

      # ---------------- Terraform Apply ----------------
      - name: Terraform Apply
        working-directory: ./parent
<<<<<<< HEAD
        run: terraform apply -auto-approve tfplan
=======
        env:
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          TF_VAR_vm_admin_password: ${{ secrets.AZURE_VM_ADMIN_PASSWORD }}
        run: terraform apply -auto-approve tfplan
>>>>>>> 0f12c11 (Add Terraform plan approval and apply pipeline)
